//===-- WORUYUSubtarget.h - Define Subtarget for the WORUYU -----------*- C++ -*-===//
//
// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
// See https://llvm.org/LICENSE.txt for license information.
// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
//
//===----------------------------------------------------------------------===//
//
// This file declares the WORUYU specific subclass of TargetSubtargetInfo.
//
//===----------------------------------------------------------------------===//

#ifndef LLVM_LIB_TARGET_WORUYU_WORUYUSUBTARGET_H
#define LLVM_LIB_TARGET_WORUYU_WORUYUSUBTARGET_H

#include "WORUYUFrameLowering.h"
#include "WORUYUISelLowering.h"
#include "WORUYUInstrInfo.h"
#include "WORUYURegisterInfo.h"
#include "WORUYUSelectionDAGInfo.h"
#include "llvm/CodeGen/GlobalISel/CallLowering.h"
#include "llvm/CodeGen/GlobalISel/InstructionSelector.h"
#include "llvm/CodeGen/GlobalISel/LegalizerInfo.h"
#include "llvm/CodeGen/RegisterBankInfo.h"
#include "llvm/CodeGen/SelectionDAGTargetInfo.h"
#include "llvm/CodeGen/TargetSubtargetInfo.h"
#include "llvm/IR/DataLayout.h"
#include "llvm/Target/TargetMachine.h"

#define GET_SUBTARGETINFO_HEADER
#include "WORUYUGenSubtargetInfo.inc"

namespace llvm {
class StringRef;

class WORUYUSubtarget : public WORUYUGenSubtargetInfo {
  virtual void anchor();
  WORUYUInstrInfo InstrInfo;
  WORUYUFrameLowering FrameLowering;
  WORUYUTargetLowering TLInfo;
  WORUYUSelectionDAGInfo TSInfo;

private:
  void initializeEnvironment();
  void initSubtargetFeatures(StringRef CPU, StringRef FS);

protected:
  // unused
  bool isDummyMode;

  bool IsLittleEndian;

  // whether the cpu supports jmp ext
  bool HasJmpExt;

  // whether the cpu supports jmp32 ext.
  // NOTE: jmp32 is not enabled when alu32 enabled.
  bool HasJmp32;

  // whether the cpu supports alu32 instructions.
  bool HasAlu32;

  // whether we should enable MCAsmInfo DwarfUsesRelocationsAcrossSections
  bool UseDwarfRIS;

  // whether we allows misaligned memory access
  bool AllowsMisalignedMemAccess;

  // whether cpu v4 insns are enabled.
  bool HasLdsx, HasMovsx, HasBswap, HasSdivSmod, HasGotol, HasStoreImm,
      HasLoadAcqStoreRel, HasGotox;

public:
  // This constructor initializes the data members to match that
  // of the specified triple.
  WORUYUSubtarget(const Triple &TT, const std::string &CPU, const std::string &FS,
               const TargetMachine &TM);

  WORUYUSubtarget &initializeSubtargetDependencies(StringRef CPU, StringRef FS);

  // ParseSubtargetFeatures - Parses features string setting specified
  // subtarget options.  Definition of function is auto generated by tblgen.
  void ParseSubtargetFeatures(StringRef CPU, StringRef TuneCPU, StringRef FS);
  bool getHasJmpExt() const { return HasJmpExt; }
  bool getHasJmp32() const { return HasJmp32; }
  bool getHasAlu32() const { return HasAlu32; }
  bool getUseDwarfRIS() const { return UseDwarfRIS; }
  bool getAllowsMisalignedMemAccess() const {
    return AllowsMisalignedMemAccess;
  }
  bool hasLdsx() const { return HasLdsx; }
  bool hasMovsx() const { return HasMovsx; }
  bool hasBswap() const { return HasBswap; }
  bool hasSdivSmod() const { return HasSdivSmod; }
  bool hasGotol() const { return HasGotol; }
  bool hasStoreImm() const { return HasStoreImm; }
  bool hasLoadAcqStoreRel() const { return HasLoadAcqStoreRel; }
  bool hasGotox() const { return HasGotox; }

  bool isLittleEndian() const { return IsLittleEndian; }

  const WORUYUInstrInfo *getInstrInfo() const override { return &InstrInfo; }
  const WORUYUFrameLowering *getFrameLowering() const override {
    return &FrameLowering;
  }
  const WORUYUTargetLowering *getTargetLowering() const override {
    return &TLInfo;
  }
  const WORUYUSelectionDAGInfo *getSelectionDAGInfo() const override {
    return &TSInfo;
  }
  const WORUYURegisterInfo *getRegisterInfo() const override {
    return &InstrInfo.getRegisterInfo();
  }
};
} // End llvm namespace

#endif
